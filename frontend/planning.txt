Root COMPONENT:
- Contains other components

Home COMPONENT:
- Button to select player vs bot
    - Render PlayerVsBot
- Button to select player vs player
    - Render to PlayerVsPlayer

PlayerVsPlayer COMPONENT:
- Establish websocket connection to /game
- Contains PlayerLobby, Room, Game components
- Listen to error events and respond with a pop up dialogue at the top
    - PlayerLobby COMPONENT:
        - Show a dialogue for the user to enter a name
        - Show a dialogue containing a list of rooms
            - Add options:
                - Clicking a room request join-room in the backend and render room state
                - Add refresh room button
                - Add create room button
                - Add exit button
        - Upon player joining a room, render room component

    - Room COMPONENT:
        - Show a dialogue containing list of current players
        - Show current owner
        - Only show start game button to the owner
        - Display waiting for owner to start game for other people
        - Add exit button

    - Game COMPONENT:
        - Implement locking mechanism for draw card, 
        - only allow them to draw until at least 1 card match the top card

        - Gameboard COMPONENT:
            - Show button to draw card
            - Show current top card
            - Show current color
            - If current color is black then show additional enforced color
        - OtherPlayer COMPONENT:
            - Show username
            - OtherHand COMPONENT:
                - Show other's current hand but hidden
                - Limit of 7
        - CurrentPlayer COMPONENT:
            - Show username
            - Hand COMPONENT:
                - Show player's current hand
                - Onclick add to PlayHand component
                - Add prev and next button to show overflow cards
            - PlayHand COMPONENT:
                - Show player's card they want to play
                - Add functionality to remove the card they want to play
                - Show play cards button
                - Show UNO button (act as play cards but with uno called this is intentional)
                - Add check to show choose a color dialogue if last card on playHand is wild
                - Add prev and next button to show overflow cards
        
    - GameEnd COMPONENT:
        - Show game finished dialogue
        - Show player currently in room (with their scores to be implemented later)
        - Show button to go back to Home
        - Show button to continue the Game (only owner can see this button, other player sees a waiting message)

// ANIMATIONS:

    - When player start the game:
        - Play cards getting distributed to every player from the middle of Gameboard 
        - It follows an order, from the first to the last player following a circle
    - When player play cards:
        - Announce to every player that these cards are played:
            - The cards will fly from that player's hand into a container above gameboard, visible to all 
            - Then, it will fly from the container to the discard pile 1 by 1.
        - IMPLENTATION:
            - Tweak play-cards response event to include cards has been played
            - Only show the container on play-cards, other events doesnt count
            - Do this for CurrentPlayer first

            - THE FLOW:
                - When gameboard receive an event that is play-cards:
                    - Render a timed container on top of discard pile containing the played cards received from the event
                    - Animation will play since they flies from playHand which is empty
                    - On timeout, render a container of cards inside of discard pile (stacked on top)
                    - The cards will fly from the announcing container to stack on top of the pile
                    - Then close both of the container
    - When player draw cards:
        - USE THE SAME METHOD (ActionType and state changes on gameboard) LIKE BEFORE
        - The card must first appear ontop Gameboard's drawPile first then make it disapear
        - Then setting the hand of player will automatically fly the card
    - When player switch pages:
        - TO BE ADDED
    - When player has won (removed from playerOrder):
        - TO BE ADDED
    